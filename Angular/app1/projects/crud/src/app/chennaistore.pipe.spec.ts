import { ChennaistorePipe } from './chennaistore.pipe';

describe('ChennaistorePipe', () => {
  it('create an instance', () => {
    const pipe = new ChennaistorePipe();
    expect(pipe).toBeTruthy();
  });
});
